"""
The Internet is made up of end-hosts, links and routers.

Data is delivered (in packets) hop by hop over each link in turn.

A packet is a self-contained unit, consisting of bank and header.
Network layer packets are called datagrams.
"""
